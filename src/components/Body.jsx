import React from 'react'
import { SectionWrapper } from '../wrapper'

const Body = () => {
  return (
    <div className="mt-5">
      Body
    </div>
  )
}

export default SectionWrapper(Body, "body");
